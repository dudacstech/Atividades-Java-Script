<!DOCTYPE html>
<html>
  <body>
    <script>
      let v = [],
        soma = 0;

      for (let i = 0; i < 10; i++) {
        v[i] = parseInt(prompt("Digite um número:"));
        soma += v[i];
      }

      document.write("Soma: " + soma);
    </script>
  </body>
</html>
